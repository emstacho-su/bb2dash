# bb2dash — Decision Log

Running log of design decisions and their reasoning. Append-only; newest last.

| Date | Decision | Why |
|---|---|---|
| 2026-09-02 | Grading stored as declarative rules, not weight columns | 6 courses use 4 structurally different grading models |
| 2026-09-02 | Facts (`assignments`) separated from state (`assignment_progress`) | re-sync can rewrite facts without touching Stack's planner |
| 2026-09-02 | Every fact row carries `source` + `confidence` | reconciliation only overwrites `tentative`/`inferred`; conflicts with `confirmed` get surfaced |
| 2026-09-03 | Crawler holds only the publishable key; insert-only RLS | browser-side code must never see the service key |
| 2026-09-09 | Embeddings in their own table keyed `(text_id, model, part_no)`, not a column on `bb_file_text` | re-embedding with a new model never touches extraction data; long units split without re-chunking |
| 2026-09-09 | Embedding model: **gte-small via Supabase Edge Functions** (384-dim, OSS, $0) | solves query-time embedding with the same free code path; no Docker, no API cost; upgrade = re-embed ~1.2k rows |
| 2026-09-09 | Corpus chunks embedded with `"{course} {bucket} — {file_name}: "` header; queries embedded raw | header carries context for thin slides; prefixing the query would push it away from passages |
| 2026-09-09 | `embed-corpus` persists per-part, not per-unit | edge CPU budget kills long units mid-request; per-part insert makes kills lossless and re-runs idempotent |
| 2026-09-09 | Hub retrieval default: **hybrid** (RRF) | ties vector on ranking (hit@1 9/10) but returns 10 distinct units per 10 results vs ~6; degrades to vector when FTS finds nothing |
| 2026-09-09 | `pg_net` enabled and kept | only route from sandboxed sessions to edge functions (org egress blocks supabase.co); pairs with pg_cron later |
| 2026-09-09 | Docker: skipped | Postgres+pgvector managed by Supabase; model lives in edge runtime; nothing to containerize |
| 2026-09-09 | Workflow SOP: dev on branches, push per completed task, **one PR per phase**, merge only on Stack's word | Stack's instruction |
| 2026-09-09 | GUI stack: **Next.js on Vercel + Supabase Auth**, no Docker | supersedes the recovered local "Docker-first hub" plan; pg_cron+pg_net cover the scheduler jobs; see `docs/planning/sprint-0-foundation/40_RECONCILIATION_2026-09-09.md` |
| 2026-09-09 | Electron endgame parked to a later phase, not dropped | its only unique value (open local OneDrive files, spawn `claude`) doesn't survive on Vercel; browser fallbacks (signed URLs, copy-path) ship first |
| 2026-09-09 | Renderer: CSS Modules + custom properties, **no Tailwind** | the `.dc.html` Nocturne artboards are the layout spec and port 1:1 |
| 2026-09-09 | GUI v1 scope cuts: no bell/announcements, no planner day view, **no grade display** this term, 14-day tracker window | honesty rule (no gradebook data yet) + reconciliation scope |
| 2026-09-09 | Effort model from `20_D1` §3 (19-type base, ≥5-points multiplier gate, source label per figure) | authoritative over the `21_D2` variant where they disagreed |
| 2026-09-09 | Screen workers run in **isolated git worktrees**, PM integrates by copying deliverable files | prevents 4 parallel Opus workers (and a concurrent external session) from clobbering one tree |
| 2026-09-09 | Adopted the out-of-band `hybrid_search_file_text` upgrade (relevance floor + similarity), captured as migration 018 | backward compatible (null-default param); a concurrent session applied it under a colliding `012` name; reverting good work made no sense |
| 2026-09-09 | RLS hardening (W-9) sequenced AFTER the first live preview | a policy bug during hardening must not be confused with a screen bug; also safer to tighten once, late, while a second session writes prod |
| 2026-09-09 | A second Claude session also edits bb2dash (prod + repo) | fetch-before-act, never force-push, flag anything not originated here — treat prod and branch as shared |
| 2026-09-10 | `main`'s `012_hybrid_similarity` + `013_hybrid_similarity_single_source` are the **canonical** hybrid-similarity migrations; gui-v1's `018_hybrid_similarity.sql` deleted | 018 was a stale copy of main's 012 (main's 013 further refined the function; prod holds 013); one source of truth per migration |
| 2026-09-10 | GUI v1 migrations renumbered 012–017 → **014–019** (`planner_columns`, `effort_base`, `work_items`, `course_display`, `files_current`, `sync_contract`) after merging main into `feat/gui-v1` | two sessions took 012+ concurrently; the GUI set is independent of `hybrid_search_file_text`, so slotting it after 013 yields the same final schema; `git mv` keeps history |
| 2026-09-10 | The renumber is **repo-only**; prod `schema_migrations` keeps the old names (012–017) for the same DDL — accepted name-level artifact, not drift. Never re-apply 014–019 | the objects are live once; a re-apply would fail or double-record; a rebuild in README order reproduces prod. See `docs/planning/sprint-0-foundation/41_RECONCILIATION_gui_vs_retrieval-mcp.md` |
| 2026-09-10 | W-8 palette does NOT send `min_similarity` to search v3; it keeps labelling sub-0.80 hits "keyword match" | the overlay prefers "always show something" (v3 header says as much); the floor is for agent callers (mcp-server default 0.78) |
| 2026-09-10 | RLS hardened to owner-scoped (migration 020): 21 `using(true)` authenticated policies → `auth.uid() = public.app_owner()`; storage `bb_files_auth_all` ANDed with the owner check; anon INSERT paths + service_role bypass preserved | closes W-9; only the sole owner's JWT can read/write. Verified: owner sees all rows, any other authenticated uid sees zero, anon insert + `search` still work |
| 2026-09-10 | `public.app_owner()` resolves the owner **by email** (`emstacho@syr.edu`) via a SECURITY DEFINER lookup on `auth.users`, NOT a hardcoded uid | the account was recreated once already (uid changed `e2c3c645…`→`fd0b7c9d…`); a literal uid would silently lock the sole user out on the next recreation. Email-based survives it with no migration. A hardcoded-uid draft was written and rejected. Guard: migration aborts if the email yields no user. Trade-off: a new advisor WARN (authenticated can call the SECURITY DEFINER `app_owner` via RPC) — accepted; the grant is required for policy evaluation and the function only returns the owner's own uid |
| 2026-09-10 | Phase 7 workers run on their own branches (`feat/<phase>-db`, `feat/<phase>-clients`) in separate worktrees, cut from the phase branch; the PM merges them back | refines the 2026-09-09 worktree decision: git refuses one branch in two worktrees, and per-worker branches give each worker its own push target and a clean merge instead of file copying |
| 2026-09-10 | Hybrid `snippet` is the **matched passage**, plain text: `ts_headline` with `StartSel`/`StopSel` emptied over the best embedding part's slice for FTS-arm hits, that slice's head for vector-only hits; `snippet_source` says which | the head of a 16-part syllabus answered every question with the instructor's phone number; `part_range` was stored and unused. No markup because both clients (React palette, MCP markdown) would have to escape it again; highlighting is a client concern |
| 2026-09-10 | Snippet length governed by a word budget (`MaxFragments=2, MaxWords=40, MinWords=12`), not the contract's "≤ ~400 chars"; observed 81–791 chars | ts_headline has no char cap; the frozen option string was kept and clients truncate for display. Revisit only if a client needs a hard cap |
| 2026-09-10 | Superseded files are dropped **before ranking** in all three search RPCs (`p_include_superseded boolean default false`); `search` v4 / MCP / web expose `include_superseded`, sent only when true | one current document is the safe default; stale drafts must not consume rank positions either. Sent-only-when-true for the same PGRST202 reason as `p_min_similarity` |
| 2026-09-10 | `superseded_by` seeded by data migration 022 for exactly the four stale IST.466 rows 018 named, matched by **sha256** and justified from `bb_files.notes` + `bb_raw`; 16 vs 40 deliberately NOT ordered (two locations of the same Sep 3 document — both point at 66) | the bb-classify-files pass that 018 expected never ran; ids are generated identities, content hashes are stable; an unprovable ordering would be a fabricated fact. Other near-duplicates (17, 18/19, 31/32/47) left for a general rule |
| 2026-09-10 | `part_range` is defined in **code points** (`Array.from(text)`), matching Postgres `char_length`/`substring`; `embed-corpus` v5 chunks that way, 023 clamps the one historical overrun instead of re-embedding | JS UTF-16 `.length` counted the emoji on text 276 as two chars; the parity harness shows identical part lists on BMP text and a prod dry-run reports 0 missing parts, so a re-embed would change nothing |
| 2026-09-10 | `web/` test harness is vitest + Testing Library (jsdom), versions pinned exact; coverage `include` scoped to `queries.search.ts` until screens gain tests | the app had zero tests; the palette shell needs a router + query client to render, and a 47% headline that counts untested screens describes nothing |
| 2026-09-10 | Keyword-arm snippets are cut from a part whose slice **covers the tsquery** — the highest-`ts_rank` covering part, vector-best part as tiebreak, lowest `part_no` last — else `ts_headline` over the whole unit; `part_no` = the part the snippet was cut from (null for the fallback) (migrations 024–025) | 021 used the vector-best part for every row, and `/code-review` showed on prod that half the "matched passage" rows for `attendance policy` did not contain the keyword — the old head-of-unit bug with confident, wrong provenance. Lowest-covering-part (024 alone) was deterministic but picked part 8 over the actual "Final Exam Day" line in part 16; `ts_rank` picks 16 outright |
| 2026-09-10 | Accepted +25% query time (≈22 → 27 ms, limit 12) for the ranked cover rule; the stored per-part `tsvector` that would remove it is backlog, not this phase | the cost is CPU in `to_tsvector`/`ts_rank` per covering part, buffers barely moved; well under the ~60 ms ceiling set for the palette; a schema change nobody asked for does not belong in a fix round |
| 2026-09-10 | `[notes]` handling for sliced snippets is server-side: prefix `[notes] ` when the marker precedes the slice; whole-unit fallback headlines only the text before the marker (024) | CLAUDE.md's "search UIs must scrub/label speaker notes" is a hard rule; both client scrubbers locate the marker inside the snippet, so a slice after the marker would have shown notes unlabeled. Re-using the marker keeps both clients unchanged. Latent today (no multi-part `[notes]` unit) — fixed in the same migration family that created the gap |
| 2026-09-10 | Keyword-mode (`search_file_text`) headline is plain text too (`StartSel`/`StopSel` emptied, 024); `limit` applied inside `fused` before the joins | the palette exposes Keyword mode and rendered literal `<b>` tags; ordering is fully determined by `(score, text_id)`, so limiting earlier is free and halves the joined rows |
| 2026-09-10 | MCP excerpt label depends on **mode** first, `snippet_source` second; web `similarity` is `number \| null` and null means "keyword match" | only hybrid returns `snippet_source`, so fts/vector rows were mislabelled "unit head"; a unit extracted but not yet embedded has null similarity and can only be a keyword hit — it must wear the badge, not vanish between the two labels |
| 2026-09-10 | Post-Phase 7 scope is `docs/planning/sprint-1-hub/60_REQUIREMENTS_v2.md`; course page is **Google Classroom-style** (Stream landing, Classwork by Blackboard folder, Grades, Info), timeline kept as a sub-view | Stack's direction after five clarification rounds; supersedes the T-list, D1 ranks, Sep 8 phases and the STATUS backlog |
| 2026-09-10 | Parallel phases get **non-overlapping migration ranges** (Phase 8 = 026–029, Phase 9 = 030–039) allocated in the briefs | two sessions taking 012+ concurrently forced a renumber once; a reserved range costs nothing |
| 2026-09-10 | Phase 8 owns `stage_content(uuid)`; Phase 9 owns the driver and every other stage and calls it, skipping with a recorded stage row if absent | one seam between two parallel phases, frozen in both briefs |
| 2026-09-10 | Course resolution from `bb_raw` uses `courses.bb_id` first, `bb_course_id` as fallback | `bb_raw.bb_course_id` holds Blackboard's internal id (`_569316_1`); the brief's rule matched 0 of 7 shells |
| 2026-09-10 | New views are created `security_invoker` with anon revoked; the 001–025 views (owner-executed, RLS bypass) get one fix-up migration in Phase 9 | `set role anon; select count(*) from v_work_items` returned all 152 rows despite migration 020; tables were hardened, views were not |
| 2026-09-10 | `courses.card_note` stored verbatim, capped at 280 by check constraint, rendered as text only | owner-typed; sanitising would rewrite what he wrote; React escapes on render |
| 2026-09-10 | Hand-narrowed row interfaces kept for `v_course_stream` / `v_content_tree` / `v_course_display` after regenerating `database.types.ts` | generated view types are all-nullable; the narrow interfaces are the contract and are asserted at the call site |
| 2026-09-10 | Tracker fetches 56 days, shows 14 with ◂ ▸ paging; the Home course card's "Next due" stays bounded to 14 days | reinstates the artboard's 8-week horizon without silently changing what the card claims |
| 2026-09-10 | Popouts are route-driven (`?item=assignment:<id>` / `session:<id>`), mounted once in the app layout | any screen can open one; Phase 8 tabs are routes so the query string is free |
| 2026-09-10 | IST.466's duplicate content paths left as first-wins (5 rows counted, none invented) | a disambiguator would change `path` and break the tree contract; a key change belongs to a later phase |
| 2026-09-10 | AI policy is **display only**; no enforcement in the MCP server or skills. Professional-side stub **dropped** | Stack's calls in the clarification rounds |
| 2026-09-13 | One `FileOpenAction` component owns the file Open ladder on every screen; `fileHonesty` answers `unknown` ("Not stored") for fields a source does not carry rather than asserting there is no route | four hand-copied ladders had already drifted in label and style (Phase 8 code review) |
| 2026-09-14 | GUI decision 1c amended: ☰ toggles a **course sidebar**, not a pop-down. The rail holds the course list; `.main` drops its `--content-max` cap and fills the rest of the width | Stack saw the deployed preview: the capped column was never centred, so a wide window hugged the left edge and left dead space on the right. Centring it would have widened the gap on both sides; the rail turns the space into navigation. In flow at ≥1024px, an overlay drawer below it |
| 2026-09-14 | The sidebar's side is one token, `--sidebar-side` in globals.css, read as a flex-direction (`row` = left, `row-reverse` = right) by the shell row, the drawer wrapper and the rail's hairline | right (Stack, 2026-09-14) (Google Classroom's class list), but Stack asked that it be flippable. Expressing it as a flex-direction rather than an inset keeps all three call sites on the same CSS property, so one value moves them together |
| 2026-09-14 | Sidebar open/closed lives in `html[data-sidebar]`, stamped by an inline boot script before first paint and owned by React after mount; the choice persists in `localStorage['bb2dash.sidebar']` with the viewport default (open ≥1024px) as the fallback | driving the collapse from a class name would have shown the server's default for a frame on every reload. The attribute is written during HTML parse, so the rail and the ☰ highlight are correct on frame one, and React never renders the attribute — there is no hydration mismatch to suppress |
| 2026-09-10 | `bb-files` Storage bucket **private** (migration 030); reads go through signed URLs only | professors' files were fetchable anonymously by derivable path since 2026-09-02; the web app already minted signed URLs |
| 2026-09-10 | Transform runs as SQL functions on **pg_cron inside Postgres** (`transform_tick` every 2 min), not as an edge function or a local hub | nothing leaves Supabase; sandbox egress never applies; pg_cron enabled for it |
| 2026-09-10 | The transform folds **only crawls registered on an owner-claimed `agent_requests` row** (`agent_requests.run_id`, migration 039); unregistered `bb_raw` runs are quarantined once, never folded; `bb_raw` unique on `(run_id, kind, bb_course_id)` | `bb_raw` is anon-insertable by design; without the guard two anonymous POSTs would have put attacker-chosen content on the owner's dashboard (Phase 8 security review) |
| 2026-09-10 | **All 15 public views are `security_invoker`** with anon revoked (migration 036, guard block refuses a future non-invoker view) | views created before 020 ran as owner and bypassed RLS; `v_work_items` returned 159 rows to anon |
| 2026-09-10 | Inbox resolutions carry a free-text `resolution_note` ("why") and are applied by the next transform, never directly; shapes: `conflict` `{accept}`, `stack_must_confirm`/`missing` `{value, value_type}`, `deadline`/`data_gap` `{dismissed}` | Stack wants the reasons kept for tuning the rules; the transform stays the only writer of typed facts |
| 2026-09-10 | Sync cadence: **Stack triggers**, the app does the rest; no scheduled crawl, no reminders | Duo; Stack's call. The Sync button enqueues, the `bb-sync` skill crawls inside a logged-in tab |
| 2026-09-11 | Only the newest crawl may declare a file missing (`stage_files` replay guard, 037) | replaying an older crawl after a newer one stamped 22 present files missing |
| 2026-09-11 | SECURITY DEFINER transform functions are not callable by `authenticated` (038); the app's only path to a transform is an `agent_requests` row | advisor finding; the RPC surface bought nothing |
| 2026-09-13 | `v_data_freshness` ignores `status = 'skipped'` rows and unregistered runs (migration 040; PM extended Phase 9's range by one number for it) | the Phase 9 security review showed two anonymous `bb_raw` POSTs could pin "crawl last attempt failed" on Home forever via the quarantine bookkeeping row; ical's daily skipped row did the same legitimately |
| 2026-09-13 | **Merge order: Phase 8 (PR course-dimension) before Phase 9 (PR sync-loop)**; Phase 9's post-review fixes take migrations 041–045 | 036's guard requires every public view to be security_invoker, and `v_course_display`'s flag comes from Phase 8's 028; replaying main with 030+ before 026–029 would abort at 036. Prod already holds both, so this is a repo-order rule, not drift |
| 2026-09-14 | `attention_items` dedupes only while `state = 'open'` (041); a "Keep mine" answer stands until Blackboard's value changes (042); `applied_at` is set only when a fact was written, otherwise the row stays "answered, applies on next sync" (042) | Phase 9 code review: the full-key unique index blocked the second resolution of any recurring question, and re-raising a kept conflict every sync made the Inbox unclearable |
| 2026-09-14 | iCal responses are collected by `ical_collect()` on every 2-minute tick, not by the daily poll (044) | pg_net drops `net._http_response` rows after 6 h; a daily two-phase poll could never read its own response |
| 2026-09-15 | One open `sync` request at a time: while a `queued`/`claimed` sync exists, the Sync button re-copies its command instead of inserting another row | the tick never touches `kind = 'sync'` (those need a browser), so a double press on 2026-09-14 left request 9 queued with nothing to close it; the skill only ever claims one |
| 2026-09-15 | Date-only `due_date` values are formatted as the date they are, never via `::timestamptz at time zone` (045) | the cast is midnight UTC, and New York is the previous evening, so conflict questions showed bb2dash's date one day early; only text was wrong, the applied value took a different path |
| 2026-09-14 | **Definition of done for every remaining phase = SOP gates + Stack's acceptance script** walked on the Vercel preview; sign-off once per phase, at the PR | Stack's answer in the MVP/DoD questionnaire (`docs/planning/sprint-1-hub/70_MVP_INDEX.md` §1.1); per-milestone sign-off was offered and declined |
| 2026-09-14 | **Every task carries an executable check** (test, SQL assertion, curl, screenshot diff) the worker runs itself, plus one demo line for the acceptance script; a task without a check is not a task | Stack's answer; it is what makes the task loops (check → build → verify → repeat) honest rather than a to-do list |
| 2026-09-14 | Phase 10 split: **10a** = gradebook mirror, Grades page + tab with latest scores, submission pull-back with files, web-only upload; **10b** = model, what-if (entry + target solver), one saved scenario per course, score history; courses with non-computable methods show only Blackboard's number | 10b is time-gated on real scores and on V-1; a course with no Blackboard total shows "publishes no total" until V-1's questions step says otherwise |
| 2026-09-14 | Google Calendar push is **due dates only** on one dedicated calendar; meetings are not pushed (supersedes Requirements v2 §6.2 #1) | Stack's choice; fewer events, no recurrence logic, API push (not an ICS feed, which Google refreshes only every 12–24 h) |
| 2026-09-14 | Bell: **opening the dropdown marks its items seen** (badge clears); no per-item read tracking in the MVP | Stack's choice; simplest honest semantics |
| 2026-09-14 | Electron MVP = shell (own window, taskbar icon, single instance) + desktop notifications for all three R-26 triggers + Sync button; **the OneDrive mirror is a post-MVP task** in the same phase; notifications come from a main-process **poller** with a watermark, not Realtime | Stack's choice on scope; research: background events belong to polling (sockets die across sleep), and "due tomorrow" is a clock trigger with no DB write behind it |
| 2026-09-14 | V-1 gains a **per-course questions block** (no-total courses, OCR-only files, week/session classification, ambiguous aggregations) answered by Stack inline; rows are decided from the materials, Stack may ask the professor and override; done = all seven courses signed off; verdict files carry a YAML machine block so a later automated pass can re-check | Stack's answers; research: Great Expectations' suite-as-data + docs-as-prose split, and accounting reconciliation's preparer/reviewer roles |
| 2026-09-14 | V-2: hook auto-tags with manual additions allowed (hook merges frontmatter), existing notes moved and back-filled from git, a session is concluded after 24 h without a resume; DoD = the brief's acceptance list, PM-verified | Stack's answers; research: ADR-style status ratchet (never regress; a post-sweep resume starts a new note) and pgvector's need for a GIN index because HNSW has no in-index filtering |
| 2026-09-14 | Styling direction is decided **after functionality is achieved**; Phase 13's DoD is fixed now (every screen on tokens, light + dark, Stack approves each) | Stack's answer; the DoD can be written without the direction, the direction cannot be chosen without the screens |
| 2026-09-14 | Research reports for each phase live in `docs/planning/sprint-1-hub/research/` and are cited from each brief's DoD; open questions they raised are collected in `70_MVP_INDEX.md` §5 and answered by Stack when each phase's PM session freezes its Contract | keeps the briefs the single place a worker reads, and keeps Stack's answers dated |
| 2026-09-15 | Phase 10 migration range is **046–059**: 10a used 046–051 plus 052–056 for its review round, 057–058 are slack, **059 is held for V-1's reconciliation** and 10a never takes it | 041–045 were live from Phase 9's fix rounds by the time 10a started, so the 041 figure in the session prompt was stale; Stack: "go with what works". Phase 9 needed five fix-round numbers, hence the slack |
| 2026-09-15 | Parallel PM sessions never branch or commit in the shared checkout `C:/Users/estac/projects/bb2dash`; each phase branch is its **own worktree** (`bb2dash-wt-<phase>`) and the brief is edited there | the Phase 10a and Phase 11 PM sessions edited two briefs in the same `main` working tree within 25 seconds of each other; a `checkout -b` or `add -A` in either would have carried the other's uncommitted file |
| 2026-09-15 | Gradebook mirror `bb_gradebook` is **append-per-run**, keyed `(run_id, course_id, column_id)`, reading `effectiveScore` (never `score`, null on every column); each row is classified `column_kind ∈ item / attendance / total / calc_other / letter`, a total being an `isCalc` column whose `formula.isTotalCalculation` is true or whose name matches 034's total regex; every `v_*` view reads only runs with a real `sync_runs` row | R-10; checked against the 9/14 crawl (45 columns, one total: IST.323 5/104, `Final Letter Grade` is a non-calc override column with no score). Unregistered runs are quarantined by 035/039 and must not leak into a score either |
| 2026-09-15 | An attendance column renders **among the item rows only when its linked assignment has a `component_id`** (`v_gradebook_latest.counts_toward_grade`); otherwise it sits in a collapsed "Attendance and bookkeeping columns" group with the letter and non-total calculated columns. Nothing is summed in 10a | Stack: collapsed, "however attendance may be a grade … it needs to show up correctly where it counts". Whether it counts is V-1's data (`assignments.component_id`), so the screen follows V-1's links instead of a client-side list; today every attendance row is unlinked and starts in the group |
| 2026-09-15 | `/grades` is grouped **by course** (Home card order); the popout shows **submission status, attempts and files, never a score**; Blackboard's attempt `receipt` is stored in `bb_attempts` but **not rendered**; a row with feedback and no score shows Blackboard's status verbatim (no "returned, ungraded" state); a staged file whose sha256 matches a pulled-back copy **stays listed** with a "matches" chip | Stack's answers 1–4 and 6 of 2026-09-15; the receipt "tells him nothing he acts on", feedback-without-score is an edge case outside the project's purpose |
| 2026-09-15 | **Reversal adopted (Requirements v2 §5): new Blackboard endpoints** — the crawler adds `…/gradebook/columns/{col}/attempts` and `…/gradebook/attempts/{id}/files`, records per-column HTTP status instead of throwing, and carries a `keys` probe on the first raw attempt; every `kind = 'course'` payload gains `crawler.version = 3` | R-17; the exact attempt key names cannot be verified without Stack's MFA session, so the probe settles them from the first live crawl the way `authorSource` settled the announcement author in Phase 9 |
| 2026-09-15 | Submission bytes are pulled by a new **bb-sync step 4b** in the logged-in tab (Playwright download → sha256 → Storage at `bb_file_relpath` → mirror → row update); `stage_attempts` only catalogues the files (`bucket = 'my_submissions'`, `classified_by = 'blackboard'`, `attempt_id`). The `classifier` enum gains `'blackboard'` (§6.2 #2 adopted); `bb_files.source_url` becomes nullable with a check that only `classified_by = 'stack'` rows may lack one | the crawler runs in a browser tab and cannot fetch cross-origin bytes (bbcswebdav lesson); the pull already works with Playwright (9/14 sync). A staged upload has no Blackboard URL, so `not null` had to go for exactly that row class |
| 2026-09-15 | `bb_files` anon INSERT is tightened to refuse `bucket = 'my_submissions'` and any `classified_by` of `stack` or `blackboard`; the Storage `bb_files_anon_insert` policy (bytes only) is **left as is** | an anonymous `bb_files` row would appear in Materials as one of Stack's submissions; an anonymous Storage object with no catalogue row is invisible to every screen, and the pull step uploads with the publishable key — tightening it would have needed the owner's JWT inside the skill |
| 2026-09-15 | The phase's only real crawl is **step (0) of Stack's acceptance script**, run with the new crawler before merge; workers build and test on fixtures cut from the 9/14 payloads plus a synthetic attempts payload in the frozen shape | every Blackboard login needs Stack's Duo push; this is a one-user application (Stack may teach a few others the sync method). A worker cannot hold a session, so the live proof belongs to the person who can |
| 2026-09-15 | bb-sync keeps **registering `run_id` after `bb.runAll` returns**, even though the crawler now accepts a `runId`; register-first waits until `transform_tick` requires the `calendar` row for registered runs | code review: the tick's 3-minute idle branch folds a registered run once one course row is old enough, with no completeness check, so a slow v3 crawl registered up front would be folded with one course and the idempotence guard would drop the rest forever. 039's grace window already covers the post-crawl gap |
| 2026-09-15 | A pulled-back submission file's Storage key carries an **`attempt-<id>/` segment** (`bb_file_relpath`, 052); a staged file keeps `<course>/my_submissions/<slug>/<file>`; step 4b never treats a Storage 409 as done | code review: the two kinds of file collided on the same key whenever Stack staged a file and then attached it in Blackboard under the same name, and the Blackboard row would have pointed at the staged bytes |
| 2026-09-15 | `my_submissions` rows are **exempt from `stage_files`' missing marker (053) and from `stage_gaps`' "bytes never stored" gap when they carry an `attempt_id` (054)**; step 4b reports files it could not pull in its own summary | attempt download URLs never appear in the content tree, so every pulled-back file would have been declared missing on the fold that created it, and its bytes arrive from a later step by design, so the gap would have been raised for every submission on every sync |
| 2026-09-15 | `attempts_allowed` is derived, not copied: `-1` when Blackboard's `attempts_left = -1`, else `multiple_attempts` when above 1, else 1 (055); the client's fallback uses the same rule | Blackboard encodes "single attempt" as `multipleAttempts = 0` and "unlimited" on a different field; passing the raw value produced "Attempt 2 of 1" on real data — a fabricated ceiling on the one screen whose rule is honesty |
| 2026-09-15 | Score-change counts (`scores_new`, `scores_changed`) are computed **only when the folded run is the newest registered crawl** (056, reusing 043's predicate); older runs report 0 with `older_run: true` | code review: a late-registered older crawl compared its values against a newer run's and reported stale scores as changes in the Activity feed |
| 2026-09-15 | Mirrored scores stay `numeric(9,3)` this phase (ECN.304 Attendance 83.33333 → 83.333, one value in 45) | widening the column means dropping and recreating the five views that depend on it; deferred until a score needs to be bit-exact, and noted in STATUS |
| 2026-09-16 | Phase 10a's acceptance crawl of 2026-09-16 ran with the **v2 crawler** from the shared `main` checkout, so the mirror side is verified live (48 columns, IST.323 total 14.8/104) and the attempts probe is not; Stack merged on that basis. The first post-merge `/bb-sync` is the v3 crawl that settles the attempt key names | the bb-sync skill loads `ingest/bb_crawler.js` from the checkout it runs in, and only the phase branch had v3; Stack: "looks good for now", then "merge". Nothing in the mirror depends on the attempts payload, and the stages are idempotent |
| 2026-09-15 | Calendar events are **timed at the due instant** (zero-length, `America/New_York`); a date-only due date becomes 11:59 PM that day, or the start of that day's class meeting for `project` / `exam` / `final_exam` | Stack's answer (Phase 11 Q2); the grid keeps date-only items in its all-day band because the grid shows the fact as recorded |
| 2026-09-15 | An event is **deleted only when the item no longer exists in Blackboard**: a Blackboard-linked assignment absent from the newest folded crawl of its course (`bb_last_seen` older than that crawl row's `captured_at`, migration 065), or a row that is deleted or undated; syllabus-only rows are never deleted for absence | Stack: crawls are the source of truth being appended to the db; syncs never delete facts, so the calendar mirrors Blackboard's presence rather than bb2dash's rows. The Contract's first rule compared with the fold's `started_at` and flagged 22 of 64 items absent — fixed in 065 |
| 2026-09-15 | One `bb2dash` calendar, filterable by course: the course code opens every title and each course has a fixed `colorId` | Stack's answer (Q1); Google Calendar cannot filter inside one calendar except by search and colour |
| 2026-09-15 | The calendar push has **its own pg_cron job** (`bb2dash-calendar-push`, `1-59/2`) and its own `calendar_push_runs` table; a statement trigger on `assignments` marks `app_settings.gcal_dirty`; Phase 9's `run_transform` / `transform_tick` are not redefined | Phase 10a re-creates `run_transform` (051) in the same sprint; two phases editing one function is a merge conflict by construction. `sync_runs` would also have needed a new value on the shared `source` enum |
| 2026-09-15 | The bell's seen mark is 033's `announcements.read_at`; unread = `read_at is null and not is_read`; the dropdown **and** `/announcements` clear it | Stack's answers (Q4); 033 added the column for this; a post already opened in Blackboard is not new |
| 2026-09-15 | Google OAuth secrets live in **Supabase Vault** behind two service_role-only RPCs (`calendar_secret_set`, `calendar_secrets`); `calendar-push` runs with `verify_jwt = false` and authenticates the tick's `x-push-secret` header (from Vault, constant-time compare); the refresh token is minted once by `scripts/google-consent.mjs` on Stack's machine (loopback, PKCE) and never passes through chat, the repo or a browser bundle | pg_net cannot carry a user JWT; the service key must not sit in the database; Stack asked for Vault |
| 2026-09-15 | The Cloud project is owned by Stack's personal Gmail; the `bb2dash` calendar and the consent grant are the SU Workspace account's (`emstacho@g.syr.edu`); consent screen External, published to Production | SU's Workspace does not let a student create a Cloud project inside the organisation and a Workspace account has no "no organisation" option; a Testing-state app loses its refresh token after 7 days |
| 2026-09-15 | Push set = every dated `assignments` row with `v_work_items.in_workload` true, including the five `reading`-typed IST.352 gradebook items; the `readings` table is never pushed | Stack's "readings and attendance are too noisy" (Q6) named the syllabus reading list; the five are graded Blackboard items with real due times |
| 2026-09-15 | `announcements.author` stays "not recorded" through Phase 11 | the 2026-09-14 live crawl proved the internal announcements endpoint carries none of the seven candidate creator keys; `ingest/bb_crawler.js` is Phase 10a's file this sprint |
| 2026-09-15 | R-16 dates are Inbox resolutions, not a migration: SITN presentation = **2026-11-04 15:45** (Stack's own choice; the syllabus group→date table has no 11/4 slot); IST.466 Major Project #1/#2 keep 10/20 and 11/17 `tentative` because the schedule lists both days of each pair without naming groups | keeps the transform the only writer of facts; nothing invented |
| 2026-09-15 | `/privacy` is a public route (like `/login`) | Google refuses to publish an OAuth consent screen without a homepage and privacy-policy URL; the page states what the calendar scope is used for |
| 2026-09-15 | Every calendar event body carries `status: confirmed` (calendar-push v3) | Google keeps a deleted event's id in a cancelled state; re-adding the item hit 409 and the fallback patch left it cancelled and invisible (live run 4: Google-side 61 vs mirror 62). One-time repair patched all 62; the next run issued zero writes |
| 2026-09-15 | The consent script loads the service key from `~/.claude.json` via node, opens the browser through `explorer.exe`, and sends an `sb_secret_` key as `apikey` only | `cmd /c start` cut the auth URL at the first `&`; PowerShell 5's JSON parser rejects the config's case-duplicate keys; a pasted non-service key returned 401 on the Vault RPC. Each was hit once on the live run |
| 2026-09-16 | Planner events are pushed to the `bb2dash` calendar as **ordinary events**; the kind lives in the title (`[IST 323 · ]Focus time · <title>`) and a fixed per-kind `colorId`, and a done task's title gains `✓ ` | the Calendar API accepts `outOfOffice` / `focusTime` / `workingLocation` only on a primary calendar and has no create endpoint for appointment schedules; Stack accepted kind-by-title-and-colour (69b) and chose kind colour over course colour (Q5) |
| 2026-09-16 | A planner event stores **instants plus its own IANA zone**; the web converts a wall clock to an instant once, with Temporal's `compatible` rule (a repeated fall-back time takes the earlier instant, a skipped spring-forward time moves forward), and SQL never re-derives an instant from a wall clock. Editing an event keeps its stored instants unless its date, time, zone or all-day setting changed (R2-4) | Stack's Q1: events are not always in New York. Postgres resolves a fold to standard time, the opposite of `compatible`, so two converters would disagree once a year; re-converting on every save moved a 01:30 fold event an hour on a title edit |
| 2026-09-16 | `planner_events.time_zone` must be `UTC` or an `Area/Location` name present in `pg_timezone_names`, checked by a trigger; the web sends `Intl`'s resolved name. The lookup is skipped on an update that leaves the zone unchanged (069) | Postgres also accepts POSIX strings such as `UTC+3` and reads their sign opposite to ISO, which Google and `Intl` read differently; a check constraint cannot run the lookup. The lookup scans the zone database: 47 ms per write warm (W-23), 499 ms on a cold first call (PM), so without 069 every task tick paid it; after 069 a `done` update takes 0.1–0.5 ms |
| 2026-09-16 | All-day planner events store `starts_at` / `ends_at` as local midnights in their zone with an **exclusive** end (a one-day event ends at 00:00 the next day) | Google's all-day `end.date` is exclusive; storing the same shape means the push copies dates instead of adding a day in one place and forgetting it in another |
| 2026-09-16 | `calendar_events` is keyed `(source, ref_id)`: 068 **renamed** `assignment_id` and swapped the primary key instead of adding columns, under a controlled cut-over (push switched off, 068 applied, v4 deployed, one push with zero writes to the 64 existing events, switched on; 3 min 37 s) | the frozen Contract asked for one mirror keyed by source; an additive second key would have left two names for one column forever. A one-off exception to "migrations are additive", taken with a pre-flight that proved 64/64 hashes and event ids unchanged before and after — not a precedent |
| 2026-09-16 | Kind colours in Google: Event 9 Blueberry, Task 1 Lavender, Out of office 4 Flamingo, Focus time 8 Graphite, Working location 2 Sage, Appointment slot 5 Banana (three are also course colours) | seven of Google's eleven colours already belong to courses; changing one later re-patches only that kind's events. Pending Stack's nod on the preview walk |
| 2026-09-16 | The push reads `v_calendar_push_items` and `calendar_events` **page by page**, and a run that cannot read a side completely aborts before diffing (R2-1); mirror writes that fail throw instead of being ignored (R2-2) | planner events are never pruned by date, so PostgREST's 1000-row cap is reachable; a truncated desired set would have deleted real events from Google and reported `ok` |
| 2026-09-16 | The planner-event live proof ran on Stack's real `bb2dash` calendar with SQL-inserted `bb2dash test · …` rows, all deleted and pushed away in the same sitting (Google 64 = mirror 64 at the end); web workers never write `planner_events` on prod | the push has no staging calendar; any row a dev server or script creates is on Stack's phone within two minutes |
| 2026-09-16 | Phase 11b ran beside Phase 10b (another PM session): 10b's 057–058 went live on prod from its branch while 11b's 067–069 did, so this branch's regenerated `database.types.ts` also carries 10b's objects; whichever PR merges second regenerates again | shared prod, disjoint migration ranges (10b: 057–058, review rounds 080–089; 11b: 067–072; Phase 12: 073–079) |
| 2026-09-16 | A client screen that hydrates inside a Suspense boundary and reads the persisted query cache or the clock renders a placeholder until hydrated (`useHydrated`, `useSyncExternalStore` with a false server snapshot); `/planner` is the first | `PersistQueryClientProvider` restores localStorage in an effect that runs before a Suspense boundary hydrates, so the grid rendered rows the server HTML lacked and React threw #418 on every load (proved on the preview: gone with the cache cleared, back with it). The server also reads today and now in UTC. Server-rendering the grid bought nothing: it had no rows |
| 2026-09-16 | **V-1 grading validation is stubbed for later; it no longer gates Phase 10b.** Precondition 3 of `68_PHASE10B_grade_model.md` is waived; the model runs on unvalidated rules and says so by leaving out parts with unsure links | Stack: "not a product feature as much as a data accuracy problem". 10a on `main` and 18 scored item columns met the other two preconditions |
| 2026-09-16 | **Reversal adopted (Requirements v2 §5): a grade engine with rank-weighted and normalized aggregation ships this term** (R-12), pure TypeScript in `web/src/lib/grade-model/`, every computed figure inside an "Our model" container | the §5 condition held: R-10 mirrored more than ten non-attendance scores. Labelling is the condition's second half |
| 2026-09-16 | The strict non-computable rule stands: any unscored `manual` (hand-graded) part hides the model for its course, with a sentence naming the part | Stack's answer 1, chosen knowing that on 2026-09-16 it hides the model in IST.323, ECN.304, IST.352 and GEO 103 (IST.466 has nothing graded, IST.471 is qualitative) until a hand-graded score posts or he links a scored column to the part |
| 2026-09-16 | Stack's column→part links live in **`grade_column_links`** (057), an owner table no sync touches; the model reads it before `assignments.component_id`; "Not graded" is a link too; the picker offers **leaf parts only**; V-1 folds the links into `assignments` later | Stack's answer 2. V-1 owns `assignments.component_id`, so an override layer keeps the seam. Leaf-only came from the code review: an item linked to a parent part was silently dropped from the model |
| 2026-09-16 | A part is **left out of the model while any counted item's link is `tentative` or `inferred`**; a picker link counts as confirmed; zero-point items never mute; a placeholder (no Blackboard column) cannot be confirmed from the app | Stack's answer 3 ("hide unsure parts"). Zero-point series placeholders would otherwise mute ECN.304 and GEO 103 quizzes for no reason |
| 2026-09-16 | The headline is **graded so far**, with zeros-on-the-rest and best case on one line under it | Stack's answer 4. It reproduces IST.323's Blackboard total, whose formula says `"running": true` (9.8 exam + 5.0 quizzes = 14.8) |
| 2026-09-16 | Blackboard's running-total setting is **read from the total column's formula** (`v_grade_model_total.bb_running`), not stored on `grading_schemes` and not asked | the flag is in the payload; V-1's table stays untouched; the DoD's "recorded, never guessed" is met by the view |
| 2026-09-16 | IST.323 extra credit raises earned points, never possible: the course can read above 100 % | its syllabus: "104 points possible, graded out of 100" |
| 2026-09-16 | `grade_components.normalize_to` is the **component's** point target, not a per-item denominator; the Contract line `f = score / (normalizeTo ?? possible)` was wrong | live IST.323 quizzes carry `normalize_to = 5.00` = the part's points; the per-item reading gave quizzes 10 instead of Blackboard's 5.0 |
| 2026-09-16 | A placeholder with no points, a confirmed link and a fraction-based part (`single`, `average`, `average_drop_lowest`, `rank_weighted`, `normalized`) takes a what-if **as a percentage** | 21 placeholders carry no points (V-1's data), including ECN.304's three exams, so acceptance step 5 had nothing to type in; a percentage needs no invented point value |
| 2026-09-16 | Surplus placeholders (more rows than `count_expected`) are dropped **earliest due first, undated last** | the Contract's "latest first" kept the seeded `lab-1` beside the real Lab #1 column and dropped Lab #4 (code review, live IST.323 shape) |
| 2026-09-16 | Screens take what-if targets, muted parts and dropped placeholders from the engine's **`itemStates()`**, never from their own copy of the rules | the web layer's copy had already drifted from the engine on two rules within one build round (code review) |
| 2026-09-16 | Scenario saves are **serialized per course** and built from the current cache at save time; a failed save refetches rather than restoring an older snapshot | code review: whole-map upserts from a stale cache could drop a value typed between two in-flight saves |
| 2026-09-16 | A phase PR's `database.types.ts` carries **only its own schema objects** while another phase's migrations are live on prod but unmerged | 10b's regeneration picked up Phase 11b's `planner_events` and a changed `calendar_events`, which no migration on 10b's tree defines; whichever PR merges second regenerates |
| 2026-09-16 | Phase 10b's browser walk: **four findings fixed in PR #15 (round 3)** — grades-table cells kept as table cells (a 10a `display: flex` on `th`/`td` misaligned every Grades table, production included), parts counted as graded from real scores only, a left-out part names the unsure items to confirm (`ItemStates.unsureItemKeys`), score history formatted like the score cell; **three carried to Phase 13** as named exceptions to its no-layout-change rule (phone-width overflow, per-exam rank weights, favicon) | Stack's call after the PM walked all seven acceptance steps on the preview in a logged-in browser (7 of 7 passed; prod restored to 0 scenarios / 0 links) |
| 2026-09-16 | **Phase 12 started; Contract C-1..C-13 frozen in `80_PHASE12_electron.md`.** Stack's answers: launch-at-login out; the poller reuses the web session's cookie and never refreshes; 15-minute cadence; **tray for the MVP** (close hides, Quit exits) with the Google consent-screen icon (`desktop/build/icon.png`); one "due tomorrow" check a day at 18:00 New York; click-only toasts; the Sync button **runs** `claude "/bb-sync <id>"` in Windows Terminal instead of prefilling it | the shell watches the renderer's own `agent_requests` POST, so `web/` stays untouched; running the command removes the PSReadLine dependency; `pwsh` is not installed, Windows PowerShell 5.1 is the host |
| 2026-09-16 | **Direction: after development, bb2dash migrates from this laptop into containers (R-28).** Phase 12's OneDrive file mirror is **dropped**; `desktop/src/core/` is plain Node with no `electron` import behind `Notifier` / `Launcher` / `WatermarkStore` adapters, so the poller and the sync launcher port as-is | Stack, Q7 and Q9: "mirroring to OneDrive could be unnecessary"; "this would need to be portable to dockers eventually". Narrows the 2026-09-09 no-Docker decision to development time only |
| 2026-09-16 | **Phase 12b added** between 12 and 13: a page-by-page pass driven by Stack's own list of bugs and changes; the PM session triages and researches before it plans, and every item ends fixed, deferred or declined (`80c_PHASE12B_page_pass.md`) | phases 8–12 were each walked against their own script; nobody has walked the whole product, and styling (13) should not paint over known bugs |
| 2026-09-16 | **Phase 14 = R-28 containers, planned** (`82_PHASE14_containers.md`): portability first on this laptop; Supabase and Vercel stay managed, no local Supabase; scope = bb2dash sync + scripts, materials MCP, harness jobs, a Claude Code dev container; vault leaves OneDrive for a private git repo; `.env`-style secrets folder + Docker secrets; scheduler in a container; per-repo compose + umbrella repo; Electron Sync button only queues after acceptance; $0 and the Windows path stays as fallback until acceptance | Stack's answers to four question rounds, 2026-09-16 |
| 2026-09-16 | Phase 14 research calls, proposed (frozen when its PM session runs): the sync becomes a **deterministic Node + Playwright `sync-runner`**, no LLM in the common path; browser and driver share one container with noVNC for the Duo login; the scheduler is an internal-timer process with missed-run catch-up, not Ofelia/cron; the scheduled sync is a pg_cron-queued `agent_requests` row; images build locally, no registry or CI; Claude in containers uses the subscription token from `claude setup-token` | R2: every branch of `skills/bb-sync` steps 1–5 is an `if` on a known value. R1: Chromium ≥ M113 binds CDP to 127.0.0.1 only. R4: cron containers need the Docker socket and do not catch up after sleep; GitHub's free tier is 500 MB shared with GHCR. R2 §1 quotes the policy covering a user's own subscription in the unmodified binary |
| 2026-09-17 | **Phase 12 built: Electron pinned to 44.4.1** (exact, not ranged); unpacked `electron-builder --dir` build with `asar: false`; the AppUserModelID `su.stack.bb2dash` is written on both the Desktop and the Start Menu shortcut | a shell whose job is to be stable should not pick up a new Chromium on install; an unpacked build is meant to be inspectable; toasts fail silently without the AUMID and setting it on both keeps one taskbar identity |
| 2026-09-17 | Config gains **`syncDryRun`** (`BB2DASH_SYNC_DRY_RUN=1`): the sync terminal prints the command instead of running it. The sync-terminal dedupe is an **in-process set**, not `firedKeys`. The menu bar **auto-hides** (Alt shows it) rather than being removed | the Sync button can be shown without starting a live Blackboard sync; only the process that saw the POST can act on it, and the watermark file stays the poller's alone; Reload stays reachable after a Vercel deploy |
| 2026-09-17 | The grade read uses a **six-hour overlap behind `lastSeenAt`**, deduped by `firedKeys`, and pages past 200 rows; first launch still fires nothing historical | `v_gradebook_history.seen_at` is the crawl time, but the transform inserts rows minutes later, so a time-only watermark silently lost grades on the normal sync path (code review R2-1/R2-2). The overlap and C-7's 500-key cap are now coupled; 500 observations in six hours is not a plausible gradebook |
| 2026-09-17 | When the web session is expired **and the window is hidden**, main reloads the hidden window (at most once per 10 min) so the web app's own proxy refreshes the cookie; **main still never calls the auth API**. The permission handler allows exactly `clipboard-sanitized-write` for the app origin | supabase-js stops auto-refresh on a hidden page, so tray polling died about an hour after close (R2-4); `web/src/proxy.ts` runs `getUser()` on every route and writes the rotated cookie. Deny-all also blocked the Sync button's copy (R2-5) |
| 2026-09-17 | Found while reading `web/` for R2-4, **not fixed in Phase 12** (`web/` is frozen there): `proxy-session.ts` returns its two auth-guard redirects without the cookies `setAll` set, so a rotation on a redirecting request is dropped. Carried to Phase 12b | self-heals on the next request but burns a refresh-token rotation; with reuse detection that can end a session |
| 2026-09-17 | **Phase 12b reverses two 10b decisions (rows of 2026-09-16: the strict non-computable rule; zeros-on-the-rest and best case).** Stack read `docs/planning/sprint-1-hub/evidence/80e_GRADE_METHOD_COMPARISON.md` (18 dummy-data gradebooks, each with a hand-derived grade) and picked the 10b engine's arithmetic with its two silencing rules removed. An unscored hand-graded part no longer hides a course's figure and an unsure link no longer drops graded scores; both are stated instead: parts a figure does not cover are named under it, and a scored column linked to nothing is named with a pointer to the "Counts toward…" picker. Removed: what-if, target solver, saved scenarios (`grade_scenarios` stays in the DB, unused), placeholder rows, the two projections and the agrees-with-Blackboard sentence. Kept: every per-part aggregation rule, the picker, `ScoreHistory`, `v_gradebook_history` | measured mean / max error in percentage points: raw points ratio 4.17 / 20.45 (and 100 % for a qualitatively graded course), syllabus weights × scores 3.24 / 20.45, 10b engine 0.13 / 1.50 but silent on three of six real course shapes, the same engine with the gates off 0.00 / 0.00. Determinism was Stack's stated aim: the figure is a pure function of mirrored Blackboard scores, `grade_components` and column links. The fixtures are the permanent regression suite |
| 2026-09-17 | **One status vocabulary, six offered values:** not opened · in progress · submitted · graded · excused · DNF, held in `web/src/lib/progress-status.ts`. The Postgres enum keeps its nine values; `planned` folds to `not_started`, `waived` / `not_applicable` to `excused` (migration 078) | Stack's answer 5. His five lost the excused family (DNF implies fault) and he kept `submitted` distinct from `graded`. Altering an enum is not additive; labels and a data fold are |
| 2026-09-17 | **The one sanctioned sync write to planner state:** `stage_gradebook` sets `assignment_progress.status = 'graded'` when Blackboard posts a score, forward-only from not opened / in progress / submitted, never from excused or DNF, and (087) only when the score is new or changed in that run, so a deliberate revert by Stack sticks. Everything else in `assignment_progress` / `reading_progress` is still never written by a sync | Stack's answer 5 ("graded should set itself"). CLAUDE.md's project fact is amended to name the exception |
| 2026-09-17 | **Workload visibility is a fact-side flag a sync never writes:** `assignments.hidden_from_workload`, and `v_work_items.in_workload` = `required is not false` for readings. Three syllabus series placeholders and nine IST.466 case-pool readings left Undated and workload; all stay in Materials | Stack's answers 8 and 9: prep-only series and other teams' cases are not his work items, but the documents are still his to read |
| 2026-09-17 | Stack is on **Ethics Team 3** (Team 2 is his Synchrony major-case group): reading 89 "Apple vs. The FBI" dated 2026-09-24 and required (073); the practice row relabelled and moved to 2026-09-22 (079). Row ids keep their seeded `ethics-team-2-*` names | the seed misread the intro deck; ids are kept so planner state, column links and calendar event ids stay attached |
| 2026-09-17 | Opening a course **closes the courses sidebar for that navigation without writing the saved preference** | Stack's answer 10; reverses Phase 8's "in-flow sidebar stays open" on wide screens |
| 2026-09-17 | **Inbox why-notes are positioned as feedback for a later agent:** view `v_inbox_feedback` + `agent_requests.kind = 'inbox_feedback'` (ignored by `transform_tick`); no agent built. Buttons on kinds `apply_resolutions` never applies say "recorded only" | Stack's answer 16. Research found nothing had ever read `resolution_note` |
| 2026-09-17 | **A gradebook column shared on purpose is not a conflict** (084): when every assignment matched to a column already carries that `bb_column_id`, `stage_assignments` restamps them all (075) and raises nothing | IST.323's proposal + final log share one column; the question could not be answered and came back on every fold |
| 2026-09-17 | **Crawler v4 reads attempts the way Blackboard's own UI does:** column → my grade row → attempts under that grade → attempt detail with `studentSubmissionFiles` (`80f_ATTEMPTS_ENDPOINT.md`) | v3's `columns/<col>/attempts?userId=` answers 200 with no results for a student on every column (21 of 21 on 2026-09-17) |
| 2026-09-17 | **Declined: widening `numeric(9,3)` scores** (P-db-3). **Deferred: `bb_content` key change** for IST.466's duplicate folder paths (P-data-1) to the next phase that touches `stage_content` | eight dependent views recreated for one third decimal of one percentage; the key change is L and no item on Stack's list depends on it |
| 2026-09-21 | **Recurring planner events are expanded in the web layer into ordinary `planner_events` rows** (082 `planner_event_series` holds only the rule; 083/088 RPCs write the rows the client sends). `calendar-push` and `v_calendar_push_items` are untouched; edits keep row ids so Google sees patches | SQL never converts a planner wall clock (Postgres and Temporal resolve a DST fall-back differently); an occurrence that is just a row needs no second push path |
| 2026-09-21 | **A series rule (frequency, end date) is not editable after creation; Repeats is offered on create only.** To change it: delete "this and following", create a new series. Weekly = same weekday; monthly = same day-of-month, a month without that day is skipped; 52 occurrences is a refusal, never a trim | PM cut for the tail; Stack's answer 15 asks for daily / weekly / monthly, a mandatory end date, the cap and single-occurrence edits, not a rule editor |
| 2026-09-21 | **"All events" means every occurrence from now on, plus the one Stack opened.** The client cuts the RPC scope 60 s ahead (`SERIES_SCOPE_SAFETY_MS`) and writes the opened occurrence itself when it falls outside; a series edit never changes any row's `done`; a save with no changed column writes nothing and does not detach | 083 refuses a row before the server's `now()` and one refusal fails the transaction; code review TR-1, TR-2, TR-8 |
| 2026-09-21 | **On `/planner` a due item opens a small anchored popover; the full details are a page, `/course/[id]/assignment/[...assignmentId]`** (catch-all because every assignment id holds a `/`; a child-shell assignment resolves under its parent course). Every other screen keeps the `?item=` popout. One dialog layer at a time on the week grid | Stack's P-planner-5 and answer 14 |
| 2026-09-21 | **`v_work_items.due_on` is the New York calendar day of `due_at`** (089), not the UTC one | every 11:59 PM Blackboard deadline sat one day late on the planner and the Home tracker since migration 016; Google (060) already used the instant and was right; found by the tail walk |
| 2026-09-22 | **An answered Inbox row is processed once and archived: `attention_items.state = 'archived'` (090) with `archived_at`, `archived_by` and a `decision` record; `archive_attention_item()` is the only way in; `v_inbox_queue` is the worker's queue.** A state, not a second table | 041's do-not-re-ask rules read `state <> 'open'` and `state = 'resolved'`; moving rows out would bring every dismissed gap and kept name back on the next fold. `attention_keep_stands()` is widened to honour archived rows for the same reason |
| 2026-09-22 | **`/inbox-apply` is the worker 077 left a queue for** (`skills/inbox-apply/SKILL.md`, request kind `inbox_feedback`): Sonnet agents gather context (syllabus rule, course precedent, Blackboard facts, prior decisions), one Opus agent writes under rules, the session records and archives. It flags feature changes and merges, raises new questions through `raise_attention()`, and never resolves an open row. Runs as `bb-sync` step 0 and from the Inbox's "Apply answers" button | Stack's brief in the bb-sync session for request 34: "build the optimization and automation portion of the inbox feedback loop". First run = request 35: 31 archived, 7 changed, 24 recorded only, 2 raised (528, 535) |
| 2026-09-22 | **Decisions are stored twice, on purpose:** one vault note per item under `projects/bb2dash/decisions/` with `collection: bb2dash-inbox-decisions` (its own section of the rag store, queried with `search_context({collection})`), and a per-day repo log `docs/inbox-decisions/YYYY-MM-DD.md` | the rag note is what the next context stage reads as precedent; the repo log is what a PR reviewer reads. `--prune` in the nightly ingest deletes rag rows with no vault file, so the vault is the durable copy |
| 2026-09-22 | **Free-text confirmations are applied by the worker, following the course's already-confirmed rows** (component, submission channel, type), never by inventing a series or merging rows. A 0-point column that Blackboard says does not count toward the grade is confirmed with no component. IST.352 knowledge checks and readings sit under component 33 (attendance, 15%) | five hand answers on 9/22 (items 398, 486–489) were the worked example; `apply_resolutions()` cannot act on text |
| 2026-09-22 | **IST.466: Stack is Ethics Team 3 and Major Case Group 2** (Synchrony 10/20, SU IT 11/17). `courses.group_notes` and the two major-project rows (`group_key`, descriptions) corrected to match; the 9/17 row above was already right | Stack's answer to an explicit two-part question in chat, after Inbox item 535 found three records disagreeing |
| 2026-09-22 | **ECN.304 Quiz 2: the hand-entered series row (`ECN.304/quiz-02`) stays; the transform's `ECN.304/quiz-2` is deleted after its column, points and score are folded in.** Rule for the worker: a Blackboard column for a quiz that was hand-entered from an announcement is folded into the hand-entered row, with Stack's confirmation first | Stack in chat: "duplicate quiz 2 should be removed"; the series row carries the calendar event and the description, and the next fold matches the column by `bb_column_id` |
| 2026-09-22 | **Stack authorized the worker to answer every open Inbox item on his behalf, from context, or mark it outdated.** 87 items closed in one run (request 37) and the 14 file-byte gaps in the next (request 38): 12 tentative columns confirmed under their syllabus components, 1 duplicate lab merged (IST.323 lab-1, precedent 528), 17 course-map seeds recorded as "already reflected", the rest dismissed as outdated with the reason on the row. Seven questions only Stack can answer are archived with a `FLAG for Stack` in the note and listed in the run's `result.flagged` (SITN group, individual-presentation slot, Ethics teammates, the two Major Case days, the three IST.466 attendance columns) | Stack in chat: "Take care of all of the open inbox items now (use context to answer or simply write outdated)". A flag on an archived row keeps the ask visible without leaving the Inbox cluttered |
| 2026-09-22 | **The manual file pull (CADENCE_RUNBOOK step 4) runs from the Playwright browser:** `page.waitForEvent('download')` around an anchor click on the durable bbcswebdav URL saves the bytes; sha256, the local mirror, the Storage POST (publishable key, no `x-upsert`), `extract_text.py` under `uv run --with python-docx --with python-pptx --with openpyxl`, and `POST /rest/v1/bb_file_text` follow from a script. Storage keys drop `#`; a re-upload of an already-stored file gets its own key and supersedes the older row; a 404 marks the row superseded by its replacement | 12 of 14 missing files pulled in one sitting (request 38); files 117 and 118 are gone from Blackboard and superseded by the Week 4 schedules 149 and 150. `char_count` on bb_file_text is generated, never posted |
| 2026-09-22 | **IST.466's 2021 due dates are stale column metadata, not dates:** the professor copied the course shell and never updated the gradebook columns' due dates. Every such value is treated as an artifact (never applied, never a conflict Stack has to settle again); the rows keep their bb2dash dates or none | Stack in chat: "the 466 due dates from 2021 are due to the professor not updating their metadata". Decisions 139–146 and items 159/160 already acted on that reading |
| 2026-09-22 | **The file pull lives in the repo: `ingest/pull_files.mjs` (+ `node --test ingest/pull_files.test.mjs`)**, replacing the session scratchpad script. The browser half stays a Playwright snippet in its header; the script never writes `bb_files` itself and hands the owner one `update` per file | Stack: "Add the file-pull script to the repo's ingest folder" |
| 2026-09-22 | **Sprint 1 closed; Phase 13 (styling, R-21) skipped, not cancelled.** More development phases come first; C-1..C-3 stay parked in `docs/planning/sprint-2/parked/81_PHASE13_styling.md` | Stack: "I plan on skipping phase 13 and adding more dev phases" |
| 2026-09-22 | **Planning documents live in one folder per sprint** (`docs/planning/sprint-N-<name>/` with `briefs / research / verification / evidence / walks / parked`); file names and numbers never change, only their folder; `docs/planning/README.md` maps old paths to new. Frozen migrations keep their old-path comments | Stack wants multiple sprints; migrations are byte-frozen so the map, not the comment, is what moves |
| 2026-09-22 | **Sprint 2 planning follows the Phase 12b method** (list → ids → triage → researchers → one question batch → briefs); migration numbering continues from 091 | the method produced 41 ids, 18 answers in one message and five workers without a mid-phase question |
