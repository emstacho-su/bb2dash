# bb2dash web — client-side env. Both values are safe in a browser bundle;
# RLS is the security boundary. The service-role key never belongs here.
NEXT_PUBLIC_SUPABASE_URL=https://goultdzqcavefcgnifdy.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
